 I already have an existing React project (using Vite) with a `src` folder set up.

Please help me integrate Tailwind CSS into this project step-by-step.

Requirements:

* Install Tailwind CSS, PostCSS, and Autoprefixer
* Initialize Tailwind config
* Configure `tailwind.config.js` to scan files inside `./index.html` and `./src/**/*.{js,ts,jsx,tsx}`
* Add Tailwind directives (`@tailwind base;`, `@tailwind components;`, `@tailwind utilities;`) into the main CSS file (e.g., `src/index.css`)
* Ensure the CSS file is properly imported into the main entry file (e.g., `main.jsx`)
* Provide exact terminal commands and file content changes
* Make sure everything works correctly with Vite

Optional:

* Suggest best practices for folder structure and Tailwind usage in a scalable React project

Return the answer in a clear step-by-step format with code blocks.